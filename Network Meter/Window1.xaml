<Window x:Class="Network_Meter.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Network_Meter" xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"
        Title="Window1" Height="350" Width="525" Background="White">
  <Window.DataContext>
    <local:MainWindow/>
  </Window.DataContext>

  <Grid>
    <oxy:Plot Title="{Binding Title}" Background="{x:Null}">
      <oxy:Plot.Series>
        <oxy:LineSeries ItemsSource="{Binding Points}"/>
      </oxy:Plot.Series>
    </oxy:Plot>
  </Grid>
</Window>
